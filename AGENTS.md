## Python Virtual Environment

This repo use `uv` to manage the Python virtual environment. So for every python related operations, please make sure to activate the virtual environment first:

```bash
source .venv/bin/activate
```